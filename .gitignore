# macOS
.DS_Store
.AppleDouble
.LSOverride

# Telemetry data (don't commit personal usage data)
# Ignore entire meta directory except .gitkeep
plugins/psd-claude-coding-system/meta/*
!plugins/psd-claude-coding-system/meta/.gitkeep

# Evolve state (local per-machine, not shared)
plugins/psd-claude-coding-system/docs/learnings/.evolve-state.json

# Node modules if added later
node_modules/

# Logs
*.log

# FreshService credentials (never commit - CRITICAL SECURITY)
*.freshservice.env
plugins/psd-claude-coding-system/.freshservice.env
.freshservice.env
